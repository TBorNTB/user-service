<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
  <conversionRule conversionWord="wex"
    converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
  <conversionRule conversionWord="wEx"
    converterClass="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>
  <property name="LOG_PATTERN"
    value="${LOG_PATTERN:-%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}}){blue} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>

  <!-- 콘솔 출력 -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>${LOG_PATTERN}</pattern>
    </encoder>
  </appender>

  <!-- CloudWatch 로그 전송 -->
  <appender name="aws_cloud_watch_log" class="io.github.dibog.AwsLogAppender">
    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>INFO</level>
    </filter>
    <layout>
      <pattern>[%thread] [%date] [%level] [%logger{36}] - %msg%n</pattern>
    </layout>
    <groupName>info_hub_app</groupName>
    <streamName>user-service2</streamName>
    <region>ap-northeast-2</region>
  </appender>

  <!-- 기본 설정: 콘솔 + CloudWatch -->
  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="aws_cloud_watch_log"/>
  </root>

  <!-- AWS 메타데이터 로그 제외 -->
  <logger name="com.amazonaws.util.EC2MetadataUtils" level="ERROR" additivity="false">
    <appender-ref ref="CONSOLE"/>
  </logger>

  <!-- 프로젝트 패키지 -->
  <logger name="com.sejong.userservice" level="DEBUG"/>
</configuration>