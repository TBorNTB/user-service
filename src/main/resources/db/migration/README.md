# Flyway 마이그레이션

- 스키마를 바꿀 때마다 **새 마이그레이션 파일**을 추가해야 합니다.
- 파일명 규칙: `V{순번}__{설명}.sql` (예: `V2__add_user_phone.sql`)
  - `V` 뒤 숫자는 정수, `__`(언더스코어 두 개) 뒤에 설명.
- 순번은 반드시 증가해야 하며, 한 번 적용된 스크립트는 수정하면 안 됩니다.
- 적용 순서: 앱 기동 시 자동 실행 (기본).
